{{#each this}}
<li class="current-movie clickable" data-imdbid="{{imdbID}}">
      <img id="current-movie-img" src='{{Poster}}' alt="">
      <p class='title'>{{Title}} </p>
      <p class='title'>{{imdbRating}}</p> 
      <div class="data hidden" style="dispay:none">{{data}}</div>
</li>
{{/each}}
